export interface ColorProps {
  colors: {
    background: string;
    foreground: string;
    button: string;
    bodyText: string;
    headText: string;
    contraBtn: string;
  };
}

export interface FontProps {
  fonts: {
    text: string;
    head: string;
  };
}
