import styled from "styled-components";

export const ThemeSwitcherStyled = styled.div`
  img {
    width: auto;
    &:hover {
      scale: 1.1;
    }
    cursor: pointer;
    transition: 0.3s;
  }
`;
